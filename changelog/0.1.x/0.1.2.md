---
summary: "The Docker image builds again — the production stage's install step had lost its RUN instruction, so docker build failed to parse the Dockerfile and 0.1.1 shipped no image."
breaking: false
security: false
---

# 0.1.2 — 2026-09-19

## Fixed

- **The Docker image builds again** — the production stage's install step had lost its `RUN` instruction, so `docker build` failed to parse the Dockerfile and 0.1.1 shipped no image.
